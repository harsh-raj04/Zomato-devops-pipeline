# Backend Environment Configuration
# Generated by Ansible for {{ inventory_hostname }}

# Database Configuration
DATABASE_URL=postgres://postgres:postgres@db:5432/zomato
DB_HOST=db
DB_PORT=5432
DB_NAME=zomato
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=supersecret_jwt_key_production_{{ ansible_date_time.epoch }}

# Server Configuration
PORT=4000
NODE_ENV={{ environment }}

# CORS Configuration (allow frontend access)
CORS_ORIGIN=http://{{ ansible_host }}:3000

# TODO: In production, use strong passwords and secret keys
# Consider using Ansible Vault for sensitive values
